.aidd/cache/

# Environment secrets
.env
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.cache/

# Logs
logs/

# Local Muses runner artefacts
.muses/

# Docker
.docker/

# Data : tout gitignoré sauf bench/ (corpus de référence CC, in-repo, petit volume)
data/*
!data/bench/
!data/renpy-corpus.jsonl
!data/test-dataset-rp.jsonl

# Pipelines cache
.cache/
pipelines/**/.cache/

# Artefacts dérivés des tables — index SQLite et embeddings npy sont
# reconstruits depuis les JSONL (cf. data-format.md). Seuls les JSONL
# (source de vérité) sont versionnés.
tables/**/*.sqlite
tables/**/*.sqlite-journal
tables/**/*.npy

# Données runtime du service (stores feedback/trust/portefeuille, snapshots).
# Chemins par défaut MUSES_FEEDBACK_DIR / MUSES_SNAPSHOT_DIR ; en prod sur /data.
feedback/
snapshots/
